<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wzb.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        <!-- useGenerateKeys是希望数据库自动生成主键值
             keyProperty="id"是将数据库自动生成的主键值设置到id上-->
        insert into user (openid, name, phone, sex, id_number, avatar)
        values (#{openid}, #{name}, #{phone}, #{sex}, #{idNumber}, #{avatar})
    </insert>

    <select id="countUsersByTime" resultType="java.lang.Integer">
        select count(id) from user
        <where>
            <if test="begin != null">
                and create_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and create_time &lt;= #{end}
            </if>
        </where>
    </select>

    <select id="showPrize" resultType="com.wzb.pojo.vo.PrizeVO">
        select up.create_time, p.name from user_prize as up left join prize as p on up.prize_id = p.id order by up.create_time desc
    </select>

</mapper>